# User-User-Recommendation-System
This is a User to User Collaborative filtering Movie Recommendation System. It entails data prepocessing where we replace null values before transforming it into a sparse matrix using csr_matrix from scipy. We then implement Nearest neighbours and fit it on the data before creating a function which recommends the 20 best movies in regards to the movie selected.
